config:
  target: "https://matriculabackend.onrender.com"
  phases:
    - duration: 15
      arrivalRate: 3

scenarios:
  - flow:
      - post:
          url: "/login"
          json:
            email: "david@gmail.com"
            password: "abcd"
          headers:
            Content-Type: "application/json"
          capture:
            - json: "$.token"
              as: "token"
      - get:
          url: "/estudiante"
          headers:
            Authorization: "Bearer {{ token }}"